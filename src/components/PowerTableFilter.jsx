import React from 'react'

const PowerTableFilter = ({ filters, moreFilters, searchBar }) => {
  return (
    <nav className="row">
      Power Table Custom Nav Bar
    </nav>
  )
}

export default PowerTableFilter
